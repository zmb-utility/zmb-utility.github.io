export interface Map {
  name: string;
  image: string;
}

export interface Utility {
  id: string;
  name: string;
  description: string;
  mp4?: string; 
  poster?: string;
  instructions?: string;
}